spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paddle_db
    username: paddle_admin
    password: password1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect